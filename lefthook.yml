remotes:
  - git_url: https://github.com/JohanTran02/lefthook-configs
    configs:
      - configs/global.yml
      - configs/ts.yml

ci:
  commands:
    lint:
      run: npx ultracite fix --quiet --type-aware --type-check --format=github